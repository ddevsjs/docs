<template>
  <main class="flex flex-1 flex-col gap-2 items-center justify-center">
    <div class="flex flex-col gap-0 items-center justify-center w-96">
      <h1 class="font-bold text-4xl">{{ $t("pages.home.title") }}</h1>
      <h2 class="font-semibold text-xl text-wrap text-center">
        {{ $t("pages.home.subtitle") }}
      </h2>
    </div>
  </main>
</template>
